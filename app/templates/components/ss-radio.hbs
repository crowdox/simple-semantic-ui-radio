<input name={{name}}
       type="radio"
       disabled={{if disabled "disabled"}}
       class="hidden"
       checked={{if checked "checked"}} />
{{#if (has-block)}}
  {{yield}}
{{else}}
  <label></label>
{{/if}}